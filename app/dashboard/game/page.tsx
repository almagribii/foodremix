import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Game - Foodremix",
  description:
    "Halaman ini berisi tentang game yang bisa dimainkan pengguna ",
};

export default function NotifPage() {
  return(
    <div>
        Game Page
    </div>

  )
}
